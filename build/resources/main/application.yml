# DATA SOURCE
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testedb?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: 123

  jpa:
    hibernate:
      ddl-auto: update

  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER